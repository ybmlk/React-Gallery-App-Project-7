import React from 'react';

// Is Called when the page is loaded but just before the API finishes fetching
const Loading = () => (
  <li className='not-found'>
    <h2>Loading...</h2>
  </li>
);

export default Loading;
